interface MyProfile {
  id: string;
  name: string;
  email: string;
  username: string;
}

interface UserProile {
  id: string;
  name: string;
  email: string;
}

interface UpdateProfileRequest {
  name: string;
  email: string;
}
